import { AggregationResults } from './aggregation-results.model';

describe('AggregationResults', () => {
  it('should create an instance', () => {
    expect(new AggregationResults()).toBeTruthy();
  });
});
